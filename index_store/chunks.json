[
  {
    "id": 0,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Overview",
    "start_line": 1,
    "end_line": 7,
    "text": "# AWS RDS Primary Unresponsive / Failover\n\n**Severity:** SEV-1\n**Owning Team:** DBA / Data Services\n**Primary Escalation:** #data-services-oncall (PagerDuty schedule\n\"dba-primary\") -- page immediately, do not wait to investigate alone"
  },
  {
    "id": 1,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Symptoms",
    "start_line": 8,
    "end_line": 16,
    "text": "Applications report a total inability to connect to the database, or every\nquery times out. The RDS console or CloudWatch shows the primary instance\nas `Unavailable` or with a spike in `DatabaseConnections` errors, and\n`FreeableMemory`/`CPUUtilization` may show anomalous flat-lining rather\nthan a gradual trend, which is characteristic of an instance-level failure\nrather than a query-level problem."
  },
  {
    "id": 2,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Likely Causes",
    "start_line": 17,
    "end_line": 26,
    "text": "1. Underlying EC2/host hardware failure on the primary instance.\n2. A storage-layer issue (EBS volume degradation) affecting I/O.\n3. An accidental or scheduled maintenance event caused an unplanned reboot.\n4. Multi-AZ automatic failover has already been triggered by AWS and the\n   application is still pointed at a stale connection/DNS cache.\n5. Replication or Multi-AZ standby itself failed, leaving no healthy\n   failover target."
  },
  {
    "id": 3,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Diagnosis Steps",
    "start_line": 27,
    "end_line": 46,
    "text": "1. Check the RDS console for the instance's `Status` field and the\n   `Recent events` tab -- AWS often logs the failure reason (host\n   replacement, storage issue, failover initiated) before you have to\n   dig further.\n2. Check CloudWatch `DatabaseConnections`, `CPUUtilization`, and\n   `FreeableMemory` for the instance over the last 30 minutes to\n   distinguish a sudden hard failure from a gradual degradation.\n3. If Multi-AZ is enabled, check whether AWS has already initiated an\n   automatic failover -- the RDS endpoint DNS record may already point at\n   the new primary, but application connection pools with long-lived\n   cached DNS resolutions may still be trying the old IP.\n4. Check `pg_stat_replication` (or the MySQL equivalent) from the\n   application's last known-good connection, if available, to see replica\n   lag immediately before the incident -- this helps confirm whether the\n   standby was healthy and current at failover time.\n5. Confirm whether this coincides with a scheduled maintenance window in\n   the RDS console's `Maintenance` tab."
  },
  {
    "id": 4,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Remediation Steps (part 1/2)",
    "start_line": 47,
    "end_line": 66,
    "text": "1. **If Multi-AZ auto-failover has already occurred:** the fastest fix is almost always forcing application pods/instances to re-resolve DNS and re-establish connections -- restart application pods (`kubectl rollout restart deployment/<name>`) rather than waiting for connection pools to notice the old endpoint is dead on their own. 2. **If Multi-AZ is enabled but has not yet failed over:** manually trigger a failover from the RDS console (\"Failover\" action) or via `aws rds reboot-db-instance --db-instance-identifier <id> --force-failover`. Confirm the standby is healthy and in-sync first if time allows -- failing over to a lagging standby can lose recent writes. 3. **If Multi-AZ is not enabled (single-AZ instance):** this requires a restore from the most recent automated snapshot or point-in-time recovery, which has a real RTO measured in tens of minutes. Begin the restore immediately in parallel with continuing investigation, since there is no faster path for a genuinely failed single-AZ instance. 4. Once a healthy primary is confirmed (new endpoint responding, replica lag at zero on any remaining standby), restart or roll application pods to force fresh connections, and watch error rates"
  },
  {
    "id": 5,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Remediation Steps (part 2/2)",
    "start_line": 66,
    "end_line": 69,
    "text": "no faster path for a genuinely failed single-AZ instance. 4. Once a healthy primary is confirmed (new endpoint responding, replica lag at zero on any remaining standby), restart or roll application pods to force fresh connections, and watch error rates return to baseline. 5. Do not delete or modify the old (failed) instance until the DBA team has had a chance to investigate root cause from its logs/metrics."
  },
  {
    "id": 6,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Escalation Path",
    "start_line": 70,
    "end_line": 76,
    "text": "This runbook itself is the SEV-1 escalation -- do not attempt independent\nmanual failover on a production primary without the DBA on-call engaged,\nexcept in the auto-failover-already-happened case in remediation step 1,\nwhich is safe to act on immediately."
  },
  {
    "id": 7,
    "file": "aws-rds-failover.md",
    "doc_title": "AWS RDS Primary Unresponsive / Failover",
    "section": "Related Runbooks",
    "start_line": 77,
    "end_line": 82,
    "text": "- `database-connection-pool-exhausted.md` -- if the primary is reachable\n  but connection-starved rather than fully unresponsive.\n- `disk-space-critical.md` -- if a replica's disk filled up due to stalled\n  WAL/binlog recycling, contributing to a failed standby."
  },
  {
    "id": 8,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Overview",
    "start_line": 1,
    "end_line": 7,
    "text": "# Database Connection Pool Exhausted\n\n**Severity:** SEV-2 (degraded service, rising error rate)\n**Owning Team:** Platform / Data Services\n**Primary Escalation:** #platform-oncall (PagerDuty schedule \"data-services-primary\")\n**Secondary Escalation:** DBA on-call, escalate after 20 minutes without mitigation"
  },
  {
    "id": 9,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Symptoms",
    "start_line": 8,
    "end_line": 16,
    "text": "Application pods start returning HTTP 503s or timeouts on any endpoint that\ntouches the database. Logs show errors such as `connection pool exhausted`,\n`could not obtain connection from pool within 30000ms`, or, on Postgres,\n`FATAL: sorry, too many clients already`. Latency dashboards show p99 request\ntime climbing sharply while CPU on the app tier stays flat or low -- the\nbottleneck is connection availability, not compute."
  },
  {
    "id": 10,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Likely Causes",
    "start_line": 17,
    "end_line": 29,
    "text": "1. A slow or long-running query (missing index, lock contention, bad plan\n   after a schema migration) is holding connections open far longer than\n   normal, starving the rest of the pool.\n2. A recent deploy shipped a code path that opens a connection but does not\n   release it back to the pool on an exception (a connection leak).\n3. Traffic spike exceeded the configured pool size for the current fleet\n   size -- the pool was sized for N replicas and the fleet autoscaled past\n   that assumption.\n4. The database itself is near its own `max_connections` limit, so every\n   application pool is fighting over a shrinking shared resource."
  },
  {
    "id": 11,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Diagnosis Steps",
    "start_line": 30,
    "end_line": 48,
    "text": "1. Check the connection pool dashboard (Grafana: `db / connection-pool`)\n   for the affected service. Compare \"active connections\" vs \"pool max\" --\n   if active is pinned at max, the pool is the bottleneck.\n2. On Postgres, run `SELECT state, count(*) FROM pg_stat_activity GROUP BY\n   state;` to see how many connections are `idle in transaction` (a strong\n   signal of leaked or stuck connections holding transactions open).\n3. Identify long-running queries with `SELECT pid, now() - query_start AS\n   duration, query FROM pg_stat_activity ORDER BY duration DESC LIMIT 10;`.\n   Anything running more than a few minutes on an OLTP path is suspicious.\n4. Check recent deploys in the last 2 hours for the affected service --\n   connection leaks are very frequently introduced by a code change, not\n   infrastructure drift.\n5. Confirm whether the fleet has autoscaled recently; compare current\n   replica count against the value the connection pool size was tuned for\n   (`pool_max_size * replica_count` should stay comfortably under the\n   database's `max_connections`)."
  },
  {
    "id": 12,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Remediation Steps",
    "start_line": 49,
    "end_line": 70,
    "text": "1. **Immediate relief:** if a small number of queries are clearly stuck or\n   `idle in transaction` for an abnormal duration, terminate them with\n   `SELECT pg_terminate_backend(pid);` for the specific PIDs identified in\n   diagnosis step 3. Do not mass-kill connections without confirming which\n   ones are the actual problem.\n2. **If caused by a bad deploy:** roll back the offending deploy\n   immediately rather than attempting a hotfix under pressure. Connection\n   leaks are rarely safe to patch live.\n3. **If caused by autoscaling outgrowing pool sizing:** temporarily lower\n   `pool_max_size` per replica so that `pool_max_size * replica_count` is\n   back under 80% of the database's `max_connections`, then redeploy the\n   config. File a follow-up ticket to make this ratio dynamic.\n4. **If the database itself is saturated:** consider enabling or scaling a\n   connection pooler (e.g. PgBouncer in transaction pooling mode) in front\n   of the database so application pools multiplex onto a much smaller\n   number of real backend connections.\n5. Once mitigated, watch the connection pool dashboard for 15 minutes to\n   confirm active connections stabilize well below the pool max before\n   closing the incident."
  },
  {
    "id": 13,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Escalation Path",
    "start_line": 71,
    "end_line": 78,
    "text": "If active connections remain pinned at max for more than 20 minutes after\nstep 1-3 above, or if `pg_terminate_backend` calls are not freeing capacity,\npage the DBA on-call directly -- this may indicate replication lag, a lock\nheld by a long-running maintenance job, or a hardware-level issue on the\nprimary."
  },
  {
    "id": 14,
    "file": "database-connection-pool-exhausted.md",
    "doc_title": "Database Connection Pool Exhausted",
    "section": "Related Runbooks",
    "start_line": 79,
    "end_line": 84,
    "text": "- `aws-rds-failover.md` -- if the primary database instance itself becomes\n  unresponsive rather than just connection-starved.\n- `high-cpu-utilization.md` -- if `pg_stat_activity` shows many active\n  (not idle) queries and CPU on the DB host is also elevated."
  },
  {
    "id": 15,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Overview",
    "start_line": 1,
    "end_line": 6,
    "text": "# Disk Space Critical on Production Node\n\n**Severity:** SEV-1 if root/data volume, SEV-3 if a non-critical mount\n**Owning Team:** Infrastructure / SRE\n**Primary Escalation:** #infra-oncall (PagerDuty schedule \"sre-primary\")"
  },
  {
    "id": 16,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Symptoms",
    "start_line": 7,
    "end_line": 15,
    "text": "Alert fires from disk-usage monitoring at 90% (warning) or 95% (critical)\non a host or mounted volume. Downstream symptoms can include: write\nfailures (`No space left on device`), a database refusing new writes or\ncrashing outright, log shippers falling behind or crash-looping, and on\nKubernetes nodes, the kubelet evicting pods with `DiskPressure` taints once\nthe node crosses its configured eviction threshold."
  },
  {
    "id": 17,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Likely Causes",
    "start_line": 16,
    "end_line": 27,
    "text": "1. Uncapped or unrotated application logs filling the log partition.\n2. A runaway process writing temp files that are never cleaned up (core\n   dumps, cache directories, unbounded upload buffers).\n3. Database WAL / binlog accumulation because replication or a backup job\n   is stalled and the DB cannot recycle old log segments.\n4. Container image sprawl -- old, unused image layers accumulating on a\n   node's container runtime storage.\n5. A genuine capacity shortfall: sustained data growth has simply outpaced\n   the provisioned volume size."
  },
  {
    "id": 18,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Diagnosis Steps",
    "start_line": 28,
    "end_line": 46,
    "text": "1. SSH to the affected host and run `df -h` to confirm which mount is\n   actually full -- do not assume it is the root volume.\n2. Run `du -sh /* 2>/dev/null | sort -rh | head -15` (adjust path to the\n   full mount) to find the largest top-level consumers.\n3. If logs are suspected: check `/var/log` and any application-specific\n   log directories with `du -sh`, and check whether logrotate is actually\n   running (`cat /etc/logrotate.d/<service>` and check its last run).\n4. If it's a database host: check WAL/binlog directory size and compare\n   against replication lag -- a stalled replica or backup job will prevent\n   old segments from being recycled even though the primary is healthy.\n5. On Kubernetes nodes: run `docker system df` or `crictl imagefs info`\n   (depending on runtime) to see how much space is consumed by unused\n   image layers versus running containers.\n6. Check historical disk usage trend on the Grafana `node / disk` dashboard\n   over the last 30 days to distinguish a sudden spike from steady organic\n   growth."
  },
  {
    "id": 19,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Remediation Steps (part 1/2)",
    "start_line": 47,
    "end_line": 69,
    "text": "1. **Immediate relief (buys time, not a fix):** - Clear rotated/compressed logs older than the retention policy: `find /var/log -name \"*.gz\" -mtime +7 -delete` (confirm retention policy before running against production). - Prune unused container images: `docker image prune -af` or the `crictl`/`ctr` equivalent for the node's runtime. - Clear known-safe temp/cache directories for the affected application per its own runbook -- never delete files you cannot positively identify as safe to remove. 2. **If caused by unrotated logs:** fix or reinstall the logrotate config for the affected service and confirm it runs successfully with `logrotate -f <config>` before closing the incident. 3. **If caused by stalled WAL/binlog recycling:** resolve the underlying replication or backup stall first (see `aws-rds-failover.md` if the replica itself is down) -- deleting WAL/binlog files manually can break replication or point-in-time recovery and should only be done under DBA guidance. 4. **If it is a genuine capacity shortfall:** expand the volume (cloud block storage resize, or provision a larger disk) rather than repeatedly firefighting cleanup. File a capacity-planning follow-up. 5. Re-check `df -h` after remediation and"
  },
  {
    "id": 20,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Remediation Steps (part 2/2)",
    "start_line": 69,
    "end_line": 71,
    "text": "done under DBA guidance. 4. **If it is a genuine capacity shortfall:** expand the volume (cloud block storage resize, or provision a larger disk) rather than repeatedly firefighting cleanup. File a capacity-planning follow-up. 5. Re-check `df -h` after remediation and confirm usage has dropped below the warning threshold (typically 80%) before resolving the alert."
  },
  {
    "id": 21,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Escalation Path",
    "start_line": 72,
    "end_line": 78,
    "text": "If the volume is the root filesystem of a stateful/database node and usage\ndoes not drop after clearing safe-to-delete files, escalate to the DBA\non-call immediately -- a full root volume on a database primary can cause\nan unclean crash."
  },
  {
    "id": 22,
    "file": "disk-space-critical.md",
    "doc_title": "Disk Space Critical on Production Node",
    "section": "Related Runbooks",
    "start_line": 79,
    "end_line": 84,
    "text": "- `aws-rds-failover.md` -- when a database replica is down and unable to\n  recycle WAL/binlogs, contributing to disk growth.\n- `kubernetes-pod-crashloop.md` -- when `DiskPressure` evictions are the\n  symptom bringing you here."
  },
  {
    "id": 23,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Overview",
    "start_line": 1,
    "end_line": 6,
    "text": "# High CPU Utilization\n\n**Severity:** SEV-2 if user-facing latency is affected, SEV-3 otherwise\n**Owning Team:** Service-owning team, with Infrastructure / SRE support\n**Primary Escalation:** #infra-oncall (PagerDuty schedule \"sre-primary\")"
  },
  {
    "id": 24,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Symptoms",
    "start_line": 7,
    "end_line": 14,
    "text": "CPU utilization alert fires above 85% sustained for 5+ minutes on a host,\ncontainer, or pod. User-facing symptoms typically include elevated p95/p99\nlatency, request timeouts, and in Kubernetes, pods being throttled by CFS\n(visible as `container_cpu_cfs_throttled_seconds_total` climbing) even\nthough the pod hasn't crashed."
  },
  {
    "id": 25,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Likely Causes",
    "start_line": 15,
    "end_line": 28,
    "text": "1. A genuine, expected traffic increase that has simply outpaced current\n   replica count or instance sizing.\n2. A regression in a recent deploy -- an accidentally-quadratic code path,\n   a missing cache, an N+1 query pattern moved into a hot loop, or a change\n   that disabled an optimization.\n3. A single noisy-neighbor process or pod consuming disproportionate CPU on\n   a shared host.\n4. Garbage collection thrashing on a JVM/CLR-based service that is under\n   memory pressure, which manifests as CPU spikes rather than OOM.\n5. An infinite retry loop -- a downstream dependency degraded, and a client\n   without backoff is hammering it (and burning CPU on both sides)."
  },
  {
    "id": 26,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Diagnosis Steps (part 1/2)",
    "start_line": 29,
    "end_line": 48,
    "text": "1. Check the CPU dashboard (Grafana: `service / cpu`) to see whether the spike correlates with a traffic increase (check request-rate panel side by side) or is disproportionate to traffic. 2. Check the deploy timeline for the affected service over the last few hours -- CPU regressions are very commonly introduced by a specific commit, and correlating the spike's start time with a deploy timestamp is often the fastest diagnosis. 3. On a Linux host, run `top` or `htop` to identify which process is consuming CPU; for a more precise breakdown use `perf top` if available. 4. On Kubernetes, run `kubectl top pods -n <namespace>` to find the specific pod(s) responsible, then check `container_cpu_cfs_throttled_seconds_total` in Grafana to confirm whether throttling (not just usage) is impacting latency. 5. For JVM/CLR services, check GC logs or the runtime's memory dashboard -- if CPU rises alongside frequent full GCs, the root cause is memory pressure, not a CPU-bound code path. 6. Check error rates and retry counts on calls to downstream dependencies; a retry storm from a degraded dependency is a common CPU"
  },
  {
    "id": 27,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Diagnosis Steps (part 2/2)",
    "start_line": 48,
    "end_line": 50,
    "text": "CPU rises alongside frequent full GCs, the root cause is memory pressure, not a CPU-bound code path. 6. Check error rates and retry counts on calls to downstream dependencies; a retry storm from a degraded dependency is a common CPU root cause that is easy to miss if you only look at the service itself."
  },
  {
    "id": 28,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Remediation Steps",
    "start_line": 51,
    "end_line": 70,
    "text": "1. **Immediate relief:** scale out horizontally (increase replica count)\n   if the service supports it and the cause is traffic-proportional --\n   this buys time without needing to identify root cause first.\n2. **If caused by a recent deploy:** roll back the deploy. Do not attempt\n   to patch a CPU regression live under incident pressure.\n3. **If caused by a noisy neighbor on shared infrastructure:** cordon and\n   drain the affected node, or move the offending workload to isolated\n   capacity, then investigate the noisy process separately once the\n   incident is mitigated.\n4. **If caused by GC thrashing under memory pressure:** increase the\n   memory limit/request for the affected pods as an immediate mitigation,\n   and file a follow-up to profile actual memory usage growth.\n5. **If caused by a retry storm:** fix or disable the runaway retry loop\n   at the client, and confirm the downstream dependency it was hammering\n   has also recovered.\n6. After mitigation, watch CPU and latency dashboards for 15 minutes to\n   confirm both have returned to baseline before resolving the incident."
  },
  {
    "id": 29,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Escalation Path",
    "start_line": 71,
    "end_line": 77,
    "text": "If CPU remains elevated after horizontal scale-out and a deploy rollback,\nand no noisy neighbor or retry storm is found, escalate to the service\nowner's team lead -- this may require a profiling session that is beyond\nwhat can be done live during an incident."
  },
  {
    "id": 30,
    "file": "high-cpu-utilization.md",
    "doc_title": "High CPU Utilization",
    "section": "Related Runbooks",
    "start_line": 78,
    "end_line": 83,
    "text": "- `database-connection-pool-exhausted.md` -- if elevated CPU on a database\n  host correlates with active (not idle) query counts.\n- `kubernetes-pod-crashloop.md` -- if CPU throttling is severe enough that\n  liveness probes start failing and pods begin restarting."
  },
  {
    "id": 31,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Overview",
    "start_line": 1,
    "end_line": 7,
    "text": "# Kubernetes Pod CrashLoopBackOff\n\n**Severity:** SEV-2 if it affects all replicas of a user-facing service,\nSEV-3 if partial/single-replica\n**Owning Team:** Service-owning team, with Platform support\n**Primary Escalation:** #platform-oncall (PagerDuty schedule \"platform-primary\")"
  },
  {
    "id": 32,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Symptoms",
    "start_line": 8,
    "end_line": 15,
    "text": "`kubectl get pods` shows one or more pods in `CrashLoopBackOff` status,\nwith a rapidly increasing restart count. If it affects all replicas of a\ndeployment, the service is fully down; if partial, you'll typically see\nuneven load and elevated error rates as remaining healthy pods absorb the\ntraffic."
  },
  {
    "id": 33,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Likely Causes",
    "start_line": 16,
    "end_line": 30,
    "text": "1. Application-level startup failure -- a bad config value, a missing\n   environment variable/secret, or an unhandled exception during init.\n2. Failing readiness/liveness probe -- the app may actually be healthy but\n   the probe is misconfigured (wrong path/port, too short a timeout) and\n   Kubernetes is killing healthy pods.\n3. Out-of-memory kill (`OOMKilled`) -- the container exceeds its memory\n   limit and is terminated by the kernel's OOM killer.\n4. A bad image tag or corrupted image was deployed.\n5. Node-level `DiskPressure` or `MemoryPressure` causing the kubelet to\n   evict and reschedule pods repeatedly.\n6. A dependency the app requires at startup (database, config service) is\n   unreachable, so the app crashes during its own health-check-on-boot."
  },
  {
    "id": 34,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Diagnosis Steps",
    "start_line": 31,
    "end_line": 52,
    "text": "1. `kubectl describe pod <pod>` -- check the `Last State` and `Reason`\n   field first. `OOMKilled`, `Error`, and probe failure messages all show\n   up here and immediately narrow the cause.\n2. `kubectl logs <pod> --previous` -- the `--previous` flag is essential;\n   it shows logs from the last crashed instance, not the fresh restart\n   that has barely logged anything yet.\n3. If `Reason` is `OOMKilled`: check the pod's memory limit versus its\n   actual usage trend on the Grafana `pod / memory` dashboard leading up\n   to the crash.\n4. If probes are suspected: check the deployment manifest's\n   `readinessProbe`/`livenessProbe` config against the app's actual health\n   endpoint and port, and compare `initialDelaySeconds` against the app's\n   real startup time.\n5. Check recent deploys and image tag changes for this service -- a bad\n   rollout is one of the most common causes and is visible in\n   `kubectl rollout history`.\n6. Check node conditions with `kubectl describe node <node>` for\n   `DiskPressure` or `MemoryPressure` if crashes are correlated across\n   multiple unrelated pods on the same node."
  },
  {
    "id": 35,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Remediation Steps",
    "start_line": 53,
    "end_line": 71,
    "text": "1. **If caused by a bad deploy:** roll back immediately with\n   `kubectl rollout undo deployment/<name>` rather than debugging forward\n   under pressure.\n2. **If `OOMKilled`:** raise the pod's memory limit as an immediate\n   mitigation (`kubectl set resources` or update the manifest), then file\n   a follow-up to profile actual memory usage and right-size properly.\n3. **If a misconfigured probe:** fix the probe path/port/timeout in the\n   manifest and redeploy; do not just delete the probe as a workaround,\n   since it exists to protect real traffic from an unhealthy pod.\n4. **If a missing config/secret:** correct the ConfigMap/Secret and\n   restart the deployment (`kubectl rollout restart deployment/<name>`).\n5. **If a node-level pressure condition:** cordon the affected node\n   (`kubectl cordon <node>`) so the scheduler stops placing pods there,\n   and drain it once replacement capacity is confirmed healthy.\n6. After remediation, confirm restart count stops climbing and pods reach\n   `Running`/`Ready` status (`kubectl get pods -w`) before closing."
  },
  {
    "id": 36,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Escalation Path",
    "start_line": 72,
    "end_line": 78,
    "text": "If pods continue crash-looping after a rollback and no clear `Reason` is\nfound in `kubectl describe pod`, escalate to the platform on-call for a\ndeeper cluster-level investigation (node health, CNI issues, or control\nplane problems)."
  },
  {
    "id": 37,
    "file": "kubernetes-pod-crashloop.md",
    "doc_title": "Kubernetes Pod CrashLoopBackOff",
    "section": "Related Runbooks",
    "start_line": 79,
    "end_line": 84,
    "text": "- `high-cpu-utilization.md` -- if CPU throttling is severe enough to fail\n  liveness probes.\n- `disk-space-critical.md` -- if `DiskPressure` node conditions are\n  triggering the evictions."
  }
]